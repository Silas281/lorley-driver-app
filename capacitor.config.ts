import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.larnis.lorley',
  appName: 'Lorley',
  webDir: 'dist/lorley',
  bundledWebRuntime: false
};

export default config;
